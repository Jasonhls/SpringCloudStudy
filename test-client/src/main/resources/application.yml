server:
  port: 8793
  servlet:
    context-path: /test

spring:
  application:
    name: test-client

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8895/eureka/
  instance:
    prefer-ip-address: true